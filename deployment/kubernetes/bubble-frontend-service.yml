apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: team-magma
  labels:
    app: bubble-front
spec:
  selector:
    app: bubble-front
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
    # name: http
  type: LoadBalancer